import { SearchfiltrePipe } from './searchfiltre.pipe';

describe('SearchfiltrePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfiltrePipe();
    expect(pipe).toBeTruthy();
  });
});
